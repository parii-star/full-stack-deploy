# Express server
PORT=8787

# Postgres connection string (Coolify typically provides this)
# Example: postgres://user:pass@host:5432/dbname
DATABASE_URL=postgres://postgres:postgres@localhost:5432/expenses

# Optional: restrict which browser origin can call the API (recommended when frontend is separate)
# CORS_ORIGIN=http://your-frontend-domain
