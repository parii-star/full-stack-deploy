# Optional.
# - Local dev (Vite on 5173 + API on 8787): set this to your API origin.
# - Production (single service serving UI + API): leave unset to use same-origin `/api/...`.
VITE_API_BASE=http://127.0.0.1:8787
