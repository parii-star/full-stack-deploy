services:
  db:
    # Local only: connect with psql/DB tools from your laptop.
    ports:
      - "127.0.0.1:${POSTGRES_HOST_PORT:-5432}:5432"

  web:
    # Local dev: open http://localhost:8080
    ports:
      - "127.0.0.1:${WEB_HOST_PORT:-8080}:80"
